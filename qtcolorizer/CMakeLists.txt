
set(COLORIZER_SOURCES qtcolorizer.cpp)

add_library(${CMAKE_PROJECT_NAME} SHARED ${COLORIZER_SOURCES})
add_library(${CMAKE_PROJECT_NAME}-static STATIC ${COLORIZER_SOURCES})
set_target_properties(${CMAKE_PROJECT_NAME}-static PROPERTIES OUTPUT_NAME ${CMAKE_PROJECT_NAME})
